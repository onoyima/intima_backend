import { defineConfig } from "vite";
// This is a stub config file - not used in API-only mode
// The backend runs as a standalone API server
// The frontend is served separately by intima_frontend
export default defineConfig({
// Empty config - vite.ts is not used when running in API-only mode
});
